#!/usr/bin/make -f
export DEB_BUILD_MAINT_OPTIONS = hardening=+all

%:
	dh $@ --with autoreconf 

override_dh_auto_configure:
	dh_auto_configure -- --with-udev-base-dir=/lib/udev --disable-valgrind

override_dh_auto_install:
	dh_auto_install
	find . -name \*.md5 -delete
